d: //"change drive to d"
git status
git remote -v //"verify remote repo"
git add <filename>
git add -u
git commit -m <"comment"> //"commit localy"
git push -u origin master //"push to remote master branch"
git fetch origin master //"fetch changes from remote master branch to local"
git merge origin/master //"merge master branches"
git pull origin master //"fetch and merge for master branches"
git remote remove origin //"break connection with remote repo"
git remote add origin <address>
git pull origin master --allow-unrelated-histories
git clone <address> //"get folder from remote to local"

